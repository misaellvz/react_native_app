import React from 'react'
import { ActivityIndicator } from 'react-native'

const Spinner = () => {
  return (
    <ActivityIndicator
    size="large"
    color="#AEAEAE"
  />
  )
}

export default Spinner
